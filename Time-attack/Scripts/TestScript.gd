extends Node2D
var reverse_velocity = Vector2.ZERO
var reverse_set = false

# information needed to record vectors:
#	time when vectors change
#	the vector values
#	how long a vector stays a certain value
#	the position the vectors occur at

# information needed to record animations:
#	time when animation changes
#	the animation that was being played
#	the length of time the animaiton was played


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass

